slam_toolbox:
  ros__parameters:

    # ROS 2 / simulation
    use_sim_time: true

    # SLAM mode
    mode: mapping

    # Frames
    map_frame: map
    odom_frame: odom
    base_frame: base_footprint
    scan_topic: /scan

    # Map update
    resolution: 0.05
    map_update_interval: 2.0

    # Laser configuration
    min_laser_range: 0.12
    max_laser_range: 12.0
    minimum_time_interval: 0.5
    transform_timeout: 0.2

    # Scan matching
    minimum_travel_distance: 0.05
    minimum_travel_heading: 0.05

    # Loop closure
    do_loop_closing: true
    loop_search_maximum_distance: 3.0
    loop_match_minimum_response_fine: 0.1
    loop_match_minimum_response_coarse: 0.2

    # Solver
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # Scan buffer
    scan_buffer_size: 10
    scan_queue_size: 10

    use_sim_time: true

    #uncomment following lines and add a maps path to continue mapping on previous map

    # mode: mapping

    # map_file_name: /home/smudhan/assessment_ws/src/mobile_manipulator_description/map/map3/map3

    # map_start_pose: [0.0, 0.0, 0.0]
    # map_start_at_dock: false